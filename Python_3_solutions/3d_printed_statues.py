# 3d_printed_statues.py

n = int(input())

max_printers = n // 2 # floored division

minimum_days = 10e100
candidate_days

printer_build_days = 2 ** (i-1)

for i in range (max_printers):
  max_potential = i*n
  stat_build_days = 0
  stat_built = 0

  # while stat_build_days
  candidate_days = printer_build_days + stat_build_days

  if candidate_days < :
    minimum_days = candidate_days